"use strict";

toggleWidgetLowerPart();
toggleSidebar();

function toggleScroll() {
  var body = document.querySelector('body');
  body.classList.toggle('body--inactive');
}

function toggleWidgetLowerPart() {
  var arrow = document.querySelectorAll('.widget__arrow'),
      activationClass = 'widget__lower--inactive';
  arrow.forEach(function (elem) {
    elem.addEventListener('click', function (ev) {
      ev.target.parentNode.nextElementSibling.classList.toggle(activationClass);
    });
  });
}

function toggleSidebar() {
  var sidebar = document.querySelector('.sidebar'),
      activationClass = 'sidebar--active';

  (function () {
    var btn = document.querySelector('.header__btn');
    btn.addEventListener('click', function () {
      sidebar.classList.add(activationClass);
      toggleScroll();
    });
  })();

  (function () {
    var btn = document.querySelector('.sidebar__btn--close');
    btn.addEventListener('click', function () {
      sidebar.classList.remove(activationClass);
      toggleScroll();
    });
  })();
}

function showMoreWidgetElems() {// const widgetLower = document.querySelector('.widget-options__lower')
  // console.log(widgetLower.childNodes)
  // let arr = []
  // console.log(
  //   widgetLower.childNodes.forEach(el => {
  //     if (el.nodeName === 'LABEL') {
  //       arr.push(el)
  //     }
  //   })
  // )
  // console.log(arr)
  // for (let i = 0; i < arr.length; i++) {
  //   console.log(arr[i])
  //   if (arr[i].indexOf) console.log()
  // }
}

showMoreWidgetElems(); // const cards = document.querySelector('.cards-list')
// console.log(cards.children.length);